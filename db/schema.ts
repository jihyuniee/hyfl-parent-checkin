import { sql } from "drizzle-orm";
import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const students=sqliteTable("students",{id:integer("id").primaryKey({autoIncrement:true}),schoolYear:integer("school_year").notNull(),grade:integer("grade").notNull(),classNo:integer("class_no").notNull(),studentNo:integer("student_no").notNull(),name:text("name").notNull(),active:integer("active",{mode:"boolean"}).notNull().default(true)},t=>[uniqueIndex("students_identity_idx").on(t.schoolYear,t.grade,t.classNo,t.studentNo)]);
export const events=sqliteTable("events",{id:integer("id").primaryKey({autoIncrement:true}),title:text("title").notNull(),eventDate:text("event_date").notNull(),status:text("status").notNull().default("draft"),targetGrades:text("target_grades").notNull().default("1,2,3"),opensAt:text("opens_at"),closesAt:text("closes_at"),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
export const attendance=sqliteTable("attendance",{id:integer("id").primaryKey({autoIncrement:true}),eventId:integer("event_id").notNull().references(()=>events.id),studentId:integer("student_id").notNull().references(()=>students.id),guardianName:text("guardian_name").notNull(),relationship:text("relationship").notNull(),partySize:integer("party_size").notNull().default(1),checkedInAt:text("checked_in_at").notNull().default(sql`CURRENT_TIMESTAMP`)});
